# Demo URL Configuration

All eight live-demo links are centralized in:

`src/data/projects.ts`

Each project has:

```ts
demoUrl: ''
```

Paste the deployed URL into that field. When empty, the UI shows **Demo link coming soon** instead of rendering a broken link.
